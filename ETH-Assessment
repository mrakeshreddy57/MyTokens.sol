contract MyToken {

    // public variables here
  string public tokenName = "META"
  string public tokenAbbrv = "MIT"
  unitpublic totalSupply = 0;
    // mapping variable here
  mapping(adderess=>unit) public balances;

    // mint function
      function mint(address_address, unit_value)publiic {
        totalSupply += _value;
        balance[_adderss] +=_value;
        }

    // burn function
      function burn(address_address, unit_value) public {
        require(balances[_address]-=_value;
        }

}
